# Base de datos (PostgreSQL)
DATABASE_URL="postgresql://usuario:password@host:5432/cuentas_uber?schema=public"

# Auth secret (generar uno nuevo con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
AUTH_SECRET="tu_secreto_aqui"
